all:
	echo "PROJECT_NAME: ${PROJECT_NAME}"
	echo "PROJECT_GIT: ${PROJECT_GIT}"
	echo "PROJECT_CREDS: ${PROJECT_CREDS}"
	echo "BUILD_BRANCH: ${BUILD_BRANCH}"
	echo "DEPLOY_AUTO: ${DEPLOY_AUTO}"
	echo "BUILD_NUMBER: ${BUILD_NUMBER}"

